---
project: gtm-workshops
status: paused — landing page audit + deep-linking next
last_updated: 2026-03-29
last_commit: 2e1f991
---

<current_state>
Phase 1 (design consistency) complete and on main. No .planning/ yet — GSD setup is Phase 2.
Next task: audit and fix all 9 workshop landing pages (index.html), then add deep-linking.
</current_state>

<completed_work>
- All 9 activity-unified.html files unified and on main
- Design consistency: CheckCircle, footers, focus rings, p-3 laptop-textarea, W6/W8 structural fixes
</completed_work>

<next_task>
## Workshop Landing Page Audit + Deep-Linking

### Goal
Every workshop landing page must:
1. Show all 3 individual activity cards (numbered, with title + description)
2. Each card links directly to that activity via ?step=N deep-link
3. Visual design matches W7 (the gold standard — see screenshot description below)

### Gold Standard: W7 (week-7-gtm-channels-buyer-journey/index.html)
W7 already has the correct design:
- "← Back to All Workshops" link at top
- Workshop title (large, dark text)
- 3 orange activity cards, each with:
  - Circled number (1, 2, 3) on left
  - Bold activity title
  - Short description below
  - Full card is clickable: href="activity-unified.html?step=1" (step=2, step=3)
- "Workshop Slides" link at bottom
Use W7's index.html as the reference template for all fixes.

### Current State by Workshop (audited 2026-03-29)

| Workshop | Landing Page Status | Fix Needed |
|---|---|---|
| W4 market_entry_readiness | 3 cards + ?step=1/2/3 links | Verify titles/descriptions correct |
| W6 week-6-messaging-storytelling | 3 cards + ?step=1/2/3 links | Verify titles/descriptions correct |
| W7 week-7-gtm-channels-buyer-journey | 3 cards + ?step=1/2/3 links | REFERENCE — no changes needed |
| W8 week-8-competitive-strategy | 3 cards + ?step=1/2/3 links | Verify titles/descriptions correct |
| W9 week-9-map-buyer-stakeholders | 3 cards + ?step=1/2/3 links | Verify titles/descriptions correct |
| W1 finding_your_early_customers | 3 cards BUT no ?step= params | Add ?step=1/2/3 to each href |
| W3 positioning_basics | 3 cards BUT no ?step= params | Add ?step=1/2/3 to each href |
| W5 week-5-pricing-monetization | Single card (no individual activities) | Full rebuild to 3-card layout |
| W2 problems_worth_solving | Single "Start Workshop" button | Full rebuild to 3-card layout |

### Activity Titles + Descriptions Needed for W2 and W5
Look these up from the activity-unified.html StepIndicator steps array in each file —
the step titles and descriptions defined there are the source of truth.

### Deep-Linking: SPA Side (activity-unified.html)
W6, W7, W8, W9 index.html files already use ?step=N links.
But the activity-unified.html SPAs may not read that param on load.

Check each SPA: does it read `new URLSearchParams(window.location.search).get('step')` on mount?
- If missing, add to the useEffect that initializes state from localStorage:
  ```javascript
  const urlParams = new URLSearchParams(window.location.search);
  const urlStep = parseInt(urlParams.get('step'), 10);
  if (urlStep >= 1 && urlStep <= 3) {
      initialState.currentStep = urlStep;
  }
  ```
- URL param overrides localStorage step position (so user lands at the right activity)
- All other localStorage state (form content) is preserved

### W8 Note
W8 uses top-level currentStep (1/2/3) to render Activity1/Activity2/Activity3.
The ?step= param approach works the same way — verify it renders the right sub-component.

### Files in Scope
- 9 × index.html (one per workshop directory)
- 9 × activity-unified.html (add URL param reading if missing)

### Files Off Limits
- Root index.html (all-workshops landing page)
- test_data/, docs/, _archive/

### Suggested Approach
1. Use W7/index.html as the template
2. Fix W1 and W3 first (simple — just add ?step= to existing hrefs)
3. Rebuild W2 and W5 landing pages to match W7 pattern
4. Add URL param reading to all 9 activity-unified.html files
5. Verify W4/W6/W8/W9 titles+descriptions are accurate
6. Test: click each activity card on each landing page, confirm correct step loads

### Jules Suitability
Good Jules candidate for steps 1-5 once spec is confirmed. 18 files, well-defined.
</next_task>

<context>
- Static site, no build process. HTML + Tailwind CDN.
- GitHub Pages auto-deploys on push to main.
- localStorage preserves form state per workshop.
- W7 index.html is the gold standard reference for landing page design.
- W8 index.html may have slightly different step→activity mapping — check carefully.
</context>
